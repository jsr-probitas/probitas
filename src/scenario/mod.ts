/**
 * Scenario module - Core scenario definition types
 *
 * This module exports the fundamental types for scenario definitions.
 * These types are used by both the builder layer (for creating scenarios)
 * and the runner layer (for executing scenarios).
 *
 * @module
 */

export type * from "./types.ts";
